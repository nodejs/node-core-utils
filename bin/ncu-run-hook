#!/usr/bin/env node
'use strict';

const path = require('path');
const { runAsync } = require('../lib/run');

const hook = process.argv[2];
const hookFile = path.join(__dirname, '../components/git-hooks/', `${hook}.js`);
runAsync(process.execPath, [hookFile]);
